name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # Structure gate first (REPO-STANDARD.md), before anything below could
      # ever produce a build/dist artifact that trips the row-count check for
      # the wrong reason.
      - name: Repo structure gate (REPO-STANDARD.md)
        run: make check-repo

      # tests/smoke.sh is offline and fixture-only (no gh, no network, no
      # real keys) — it exercises exactly the dangerous paths: status
      # derivation, sync-signers rebuild-from-all, audit clean-on-armed,
      # the 3-key refusal, audit catching divergence, and the packaging/
      # layout's first-ever-arm landing under packaging/ rather than root.
      # Until 0.3.10 nothing invoked it on push — the desk that seals nine
      # repos had a good test and no trigger.
      - name: Smoke
        run: make smoke
